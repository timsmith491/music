<%= form_for(@song_user) do |f| %>
  <% if @song_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song_user.errors.count, "error") %> prohibited this song_user from being saved:</h2>

      <ul>
      <% @song_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :song_id %><br>
    <%= f.number_field :song_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
